name: picklepolls

services:
  db:
    image: postgres:18.6-bookworm
    container_name: picklepolls_db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - picklepolls-network

  django:
    image: picklepolls-django
    build:
      context: .
      dockerfile: Dockerfile

    env_file:
      - .env

    volumes:
      - ./project:/app/project

    ports:
      - "8020:8000"

    depends_on:
      db:
        condition: service_healthy

    networks:
      - picklepolls-network

    restart: unless-stopped

networks:
  picklepolls-network:
    name: picklepolls-bridge-network
    driver: bridge
